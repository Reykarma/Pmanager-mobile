// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "com.streetcode.bretter"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"
  }
}
